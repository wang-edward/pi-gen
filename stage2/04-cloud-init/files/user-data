#cloud-config
hostname: leos
ssh_pwauth: true
users:
  - name: edw
    plain_text_passwd: YOUR_PASSWORD
    lock_passwd: false
    sudo: ALL=(ALL) NOPASSWD:ALL
    groups: [adm, sudo, video, render, input, gpio, spi, i2c, audio]
    shell: /bin/bash
